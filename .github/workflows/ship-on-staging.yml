name: release-pdfa11y/webcomponents-demo
on:
  push:
    branches:
      - main

jobs:
  build-release-image:
    runs-on: ubuntu-latest
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to docker registry
        uses: docker/login-action@v3
        with:
          registry: rg.fr-par.scw.cloud/pdfa11y
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          push: true
          context: '{{defaultContext}}'
          file: ./apps/demo/docker/Dockerfile
          build-args: |
            APP_VERSION=${{ github.ref_name }}
          tags: |
            rg.fr-par.scw.cloud/pdfa11y/webcomponents-demo:latest

  deploy-on-staging:
    runs-on: ubuntu-latest
    needs: [build-release-image]
    steps:
      - uses: passeidireto/trigger-external-workflow-action@main
        env:
          PAYLOAD_TAG: ${{ github.ref_name }}
          PAYLOAD_IMAGE: 'rg.fr-par.scw.cloud/pdfa11y/webcomponents-demo'
          PAYLOAD_SERVICE: 'webcomponents'
        with:
          repository: docaxess/pdfa11y-k8s-webservices
          event: deploy_staging
          github_pat: ${{ secrets.K8S_WEBSERVICES_PAT }}

      - uses: slackapi/slack-github-action@v2.1.1
        with:
          webhook-type: webhook-trigger
          #'saas-deployment'
          webhook: ${{ secrets.DEPLOYMENT_SLACK_BOT }}
          payload: |
            {
              "text": ":rocket: https://staging-webcomponents.ipedis.com/webcomponents has been released on staging ! :tada: ( ${{ github.ref_name }} )"
            }
